• npm 설치/컴파일/확장 실행 시도까지 끝냈고, 샘플 데이터(DXF/모델/결과) 시딩 → 체크 실행 → 리포트 생성까지 완료했어. 진행 중 발견된 호환 이슈도 같이 정리했어.

  - package.json: @vscode/chat-extension-utils 버전을 0.0.0-alpha.5로 조정해 설치 가능하게 함.
  - src/mcp/invoke.ts: invokeTool 호출에 toolInvocationToken을 추가해 TS 컴파일 오류 해결.
  - mcp_server/server.py: FastMCP 버전 인자 호환 처리, structai_map_suggest_members 추가 파라미터 수용, structai_project_list/structai_compare_list_runs/structai_compare_read_run 추가.
  - mcp_server/schema.sql: UNIQUE(... COALESCE(...)) 표현식 제거로 SQLite 초기화 오류 해결.
  - 샘플 파일 생성: samples/model_members.csv, samples/analysis_results.csv, samples/design_inputs.csv, samples/plan.dxf.
  - 샘플 DB/리포트: C:\Coding_Works\StGenCodex\.structai\sample.sqlite에 시드됨, 리포트 C:\Coding_Works\StGenCodex\.structai\reports\check_run_1.md 생성됨.
  - 시드 결과 요약: dxf_artifact_id=1, model_id=1, analysis_run_id=1, check_run_id=1, check_items=10.

  참고:

  - VS Code 확장 호스트는 code --extensionDevelopmentPath=C:\Coding_Works\StGenCodex로 실행 시도했어. 창이 안 떠있으면 다시 실행하면 돼.
  - 샘플 데이터는 기본 DB가 아니라 sample.sqlite에 넣었어. UI에서 바로 보려면 structai.dbPath를 그 경로로 바꾸거나, 기본 DB로 다시 시딩해야 해.

  다음 중 어떤 걸로 갈까?

  1. 샘플 데이터를 기본 DB(.structai/structai.sqlite)로 다시 시딩해서 UI에서 바로 보이게 하기
  2. VS Code에서 structai.dbPath를 sample.sqlite로 바꾸고 Mapping/Results/Resolve/QA 패널 검증
  3. 실제 프로젝트 파일로 임포트하고 동일한 플로우 검증
