"use strict";
/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation and GitHub. All rights reserved.
 *--------------------------------------------------------------------------------------------*/
Object.defineProperty(exports, "__esModule", { value: true });
exports.Tag = void 0;
const prompt_tsx_1 = require("@vscode/prompt-tsx");
/**
 * A simple element that wraps your content in an XML-like `<tag>` of the given
 * name. It manages priorities to ensure that the tag wrappers survive as
 * long as any content survives.
 */
class Tag extends prompt_tsx_1.PromptElement {
    render() {
        // todo@connor4312: add a mechanism such that the closing tag is always present with the opening tag
        return (vscpp(vscppf, null,
            vscpp(prompt_tsx_1.TextChunk, null, `<${this.props.name}>\n`),
            vscpp(TagInner, { priority: 1 }, this.props.children),
            vscpp(prompt_tsx_1.TextChunk, null, `</${this.props.name}>`)));
    }
}
exports.Tag = Tag;
class TagInner extends prompt_tsx_1.PromptElement {
    render() {
        return vscpp(vscppf, null, this.props.children);
    }
}
